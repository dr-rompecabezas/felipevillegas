{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install && npm run build:css && DJANGO_SETTINGS_MODULE=config.settings.production python3 manage.py collectstatic --noinput && DJANGO_SETTINGS_MODULE=config.settings.production python3 manage.py shell -c \"from django.contrib.staticfiles.storage import staticfiles_storage; staticfiles_storage.url('css/tailwind.css')\""
  },
  "deploy": {
    "runtime": "V2",
    "numReplicas": 1,
    "startCommand": "DJANGO_SETTINGS_MODULE=config.settings.production python3 manage.py migrate --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:${PORT} --workers 3",
    "healthcheckPath": "/health/",
    "sleepApplication": false,
    "multiRegionConfig": {
      "us-east4-eqdc4a": {
        "numReplicas": 1
      }
    },
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
